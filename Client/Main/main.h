#pragma once

typedef void (WINAPI* KERNELGETSTARTUPINFO)(LPSTARTUPINFO);

static float* Camera_Zoom = (float*)0x016012F8;

//static	LRESULT WINAPI Window(HWND Window, DWORD Message, WPARAM wParam, LPARAM lParam);